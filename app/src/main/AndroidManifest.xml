<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.calcounter">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.CalCounter">
        <activity
            android:name="Activity.FoodItemDetailsActivity"
            android:label="@string/title_activity_foof_item_details"
            android:theme="@style/Theme.CalCounter.NoActionBar"></activity>
        <activity
            android:name="Activity.DisplayFoodsActivity"
            android:label="@string/title_activity_display_foods"
            android:theme="@style/Theme.CalCounter.NoActionBar" />
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/Theme.CalCounter.NoActionBar"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>